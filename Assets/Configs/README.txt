GoldenHook ScriptableObject configs live here.

Batch 3 will add:
  GrappleChainConfig.asset  — all chain tuning (link count, mass, joint limits, retract speed curves, damping)

Nothing hardcoded in runtime scripts. All numeric tuning goes through ScriptableObjects in this folder.
